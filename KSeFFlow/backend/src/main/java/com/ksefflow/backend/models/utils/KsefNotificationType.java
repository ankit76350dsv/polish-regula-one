package com.ksefflow.backend.models.utils;

// Severity level for in-app notifications shown in the KSeFFlow notification panel.
// Maps directly to the frontend notification badge colour scheme.
public enum KsefNotificationType {
    INFO,
    SUCCESS,
    WARN,
    ERROR
}
