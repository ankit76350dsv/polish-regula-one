package com.ksefflow.backend.config;

// Placeholder — Jackson 3 (Spring Boot 4) serializes LocalDate as ISO-8601 strings
// by default when the bundled datetime module is registered by auto-configuration.
// No manual ObjectMapper customization is required for this project.
